// Start the main app logic.
requirejs(['jquery'],function   ($) {
    //jQuery, canvas and the app/sub module are all
    //loaded and can be used here now.
    console.log('this is app.js in app/');
});